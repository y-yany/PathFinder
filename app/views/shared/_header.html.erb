<header>
  <div class="navbar bg-base-100 border-b-2 border-neutral">
    <div class="flex-1 px-2 lg:flex-none">
      <%= link_to '/', class: "text-lg font-bold" do %>
        LOGO
      <% end %>
    </div>
    <div class="flex flex-1 justify-end px-2">
      <div class="flex items-stretch">
        <%= link_to t('header.course_index'), '#', class: "btn btn-ghost"%>

        <%# ログイン済みユーザー向け %>
        <%= link_to t('header.course_create'), '#', class: "btn btn-ghost"%>
        <%= link_to t('header.sign_out'), destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-ghost"%>
      </div>
    </div>
  </div>
</header>