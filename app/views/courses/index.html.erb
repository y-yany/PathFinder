<div class="container mx-auto pt-4">
  <h1><%= t('.title') %></h1>

  <div class="grid grid-cols-4 gap-4">
    <% if @courses.present? %>
      <%= render @courses %>
    <% else %>
      <div class="mt-4">作成されたコースはありません</div>
    <% end %>
  </div>
</div>
